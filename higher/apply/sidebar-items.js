window.SIDEBAR_ITEMS = {"fn":[["ap","`ap` is a default implementation of [`Apply::apply`][Apply::apply] for any type that implements `Bind`, `Pure` and `FunctorRef`, where the contained type `A` implements `Clone`."],["lift2",""]],"struct":[["ApplyFn","An `ApplyFn` is a function from `A` to `B` wrapped in something Rust’s type system can more easily digest. Arguments for `Apply::apply()` are required to be of this type rather than an arbitrary type matching `Fn(A) -> B`."]],"trait":[["Apply","`Apply` takes an `Apply<Fn(A) -> B>` (or, rather, an `Apply<ApplyFn<'a, A, B>>` specifically) and applies it to an `Apply<A>` to produce an `Apply<B>`."]]};